[package]
name = "grok-grpc"
version = "0.1.0"
edition = "2024"

[dependencies]
tonic = "0.11"
prost = "0.12"
prost-types = "0.12"
tokio = { version = "1.0", features = ["full"] }
tower = "0.4"
anyhow = "1.0"  # For error handling
futures = "0.3"  # For streaming

[build-dependencies]
tonic-build = "0.11"
